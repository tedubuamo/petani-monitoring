import { Inter, Poppins } from "next/font/google";

export const poppins = Poppins({ weight: "600", style: "normal", subsets: ["latin"], display: "swap" });

export const inter = Inter({ weight: "400", style: "normal", subsets: ["latin"], display: "swap" });